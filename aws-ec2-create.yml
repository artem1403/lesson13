---
- hosts: localhost
  connection: local
  vars_files:
    - ./variables/main.yml
  gather_facts: false

  tasks:
    - name: Create a new EC2 key pair for Staging and Production environments
      ec2_key:
        name: "{{ key_name }}"
        region: "{{ region_name }}"
      register: ec2_key